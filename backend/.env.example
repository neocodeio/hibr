# Express server port
PORT=5000

# Supabase (service role key recommended for the backend — it bypasses RLS)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_ANON_KEY=

# Clerk webhook signing secret (whsec_...)
CLERK_WEBHOOK_SECRET=
